var intState = [{ id: 0, amount: 0, class: 0 }];

const listAccount = (state = intState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default listAccount;

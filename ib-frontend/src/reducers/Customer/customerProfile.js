var intState = {
    name: "htnguyen",
    email: "email@c",
    phone: "123",
};

const customerProfile = (state = intState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default customerProfile;
